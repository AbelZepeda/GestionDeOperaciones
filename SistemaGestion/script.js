document.getElementById("incidencia-form").addEventListener("submit", function(event) {
    event.preventDefault();  // Previene el envío del formulario para agregar validaciones

    // Aquí puedes añadir validaciones o enviar los datos a un backend
    alert("Incidencia registrada correctamente.");
});
